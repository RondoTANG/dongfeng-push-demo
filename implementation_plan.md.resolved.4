# 增强推送额度管理统计看板

针对“推送额度管理”后台，新增历史推送维度统计，并提供长周期的日期筛选功能，以辅助运营决策。

## 用户评审要求
- **统计维度**：需明确区分“模板消息推送（消耗公共额度）”与“一次性订阅推送（消耗个人额度）”。
- **时间跨度**：支持向前/向后 3 个月的日期范围查询。

## 提议的变更

### 推送调度控制台 ([dongfeng_push_system_demo.html](file:///Users/RondoT/.gemini/antigravity/brain/74ee55d2-4a58-4416-8d8b-e6d438315edc/dongfeng_push_system_demo.html))

#### [MODIFY] UI 布局与组件
- **统计区域重构**：将现有的“今日实时额度”与新增的“历史汇总统计”分栏显示。
- **新增统计卡片**：
    - 已推送汇总（总计）
    - 模板消息推送量（消耗公共池）
    - 一次性订阅推送量（消耗个人账户）
- **新增筛选工具栏**：在统计区右上方增加日期选择器，支持快捷选择最近 7 天、30 天或自定义 3 个月内范围。

#### [MODIFY] 数据逻辑 (Vue.js)
- **增加响应式状态**：
    - `statsRange`: 存储当前选中的日期范围。
    - `pushedStats`: 包含模板消息和订阅消息的模拟汇总数据。
- **计算属性/监控**：当日期改变时，更新模拟统计数值（体现数据交互感）。

## 验证计划

### 自动化/手工验证
- 检查新统计卡片的 UI 适配情况（响应式布局）。
- 验证日期筛选器交互是否顺畅，且数值是否有对应反馈。
- 确认术语一致性（确保新加的统计项名称符合 PRD 标准）。
