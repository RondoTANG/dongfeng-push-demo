# 推送看板布局重构与交互增强计划

根据用户反馈，优化统计看板的信息架构，将关键指标上移并引入交互式详情展开机制。

## 用户评审要求
- **实时监控重构**：“今日大盘可用余额”移至标题旁；主指标行仅保留 3 项；增加点击“今日已推送量”展开明细。
- **历史统计重构**：采用类似的 2+2 分类统计逻辑，优化信息层级。

## 提议的变更

### 推送调度控制台 ([dongfeng_push_system_demo.html](file:///Users/RondoT/.gemini/antigravity/brain/74ee55d2-4a58-4416-8d8b-e6d438315edc/dongfeng_push_system_demo.html))

#### [MODIFY] Vue 逻辑
- **新增交互状态**：
    - `showTodayDetail`: 控制今日推送明细显隐。
    - `showHistoryDetail`: 控制历史分类统计明细显隐。
- **数据结构调整**：优化统计数据的聚合逻辑。

#### [MODIFY] HTML 模板
- **标题行优化**：
    - 使用 `flex justify-between` 实现标题与核心余额指标的并列展示。
- **主指标栅格系统**：
    - 当日监控：催办池 / 今日锁定 / 今日已推送 (3列)。
    - 点击“今日已推送”卡片触发 `showTodayDetail` 切换。
- **折叠明细层设计**：
    - 使用 Vue 的 `v-if` 或 `v-show` 配合 Tailwind 的过渡动画实现平滑展开。
    - 明细层展示：模板消息实推 / 订阅凭证实推。
- **历史板块对齐**：
    - 采用一致的分类聚合逻辑，将 4 个子项按“触达渠道”和“业务类型”进行 2+2 逻辑拆解。

## 验证计划

### 手工验证
- 检查“当日实时监控”标题旁是否正确显示可用余额。
- 点击“今日已推送”卡片，验证下方是否通过动画弹出明细。
- 检查历史统计板块是否按照 2+2 逻辑结构化展示。
