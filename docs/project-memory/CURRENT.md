# 当前状态

> 只保留当前有效信息。最后更新：2026-09-04。

## 当前目标

- 围绕 2026-07-24 会议确认的核心主线推进交付，不让早期完整原型干扰当前优先级。

## 已确认事实

- 当前战略从粗放刷量转向阵地声量、真实互动和精准分发。
- 刚性主线依次为：作业配置自定义、AI 审核覆盖与纠错、分发/标签、党支部看板与资源隔离。
- 成长体系规划与原型完整，但最新会议明确当前暂停。
- 项目是多类文档、原型、Demo、Prompt、工作流和样本的集合，不是单一可统一构建的应用。
- 根 `.agents/AGENTS.md` 是 Antigravity 留下的业务与原型规则入口。

## 最近完成

- `AI热点追踪PoC/prd/供应商热点数据采集与交付标准.md`已重构为可直接发给供应商的“热点识别数据采集与交付要求”：供应商负责分平台主动发现、指定账号保底与溯源、原始内容/账号/互动指标采集、连续快照和数据SLA；护卫军负责事件聚合、热点计算、热点源分析、AI研判和作业决策。供应商自有榜单、聚类或热度分仅作可选参考，不能替代原始数据和可审计口径。
- `AI热点追踪PoC/prototype/`已完成可运行的本地后台原型：FastAPI＋SQLite负责真实持久化，豆包搜索、Codex补证、来源清洗、事件聚合、人工研判和作业草案审批形成可演示链路；Codex每3小时心跳自动化`东风热点线索采集PoC`已启用，自动任务只创建待审核结果，不直接下发正式作业。
- 业务问题保留两条并行的前置路径：一是判断哪些热点值得发“原创增长”作业，借势获取传播；二是直接选择热点事件中的具体文章或视频，生成“热点源内容加热”作业。同时补齐原创增长的后续分支：原创实际发布后登记链接、接收同口径指标快照、观察传播增量，由运营判断是否生成“原创后二次加热”草案。
- 正式PRD、业务汇报稿、可视化HTML、配置契约和后台原型均已同步“两个前置入口＋一个原创后效分支”。热点源内容加热绑定`target_source_id`；原创后二次加热绑定实际发布记录`target_submission_id`和触发判断，三类草案分别审批，均不自动执行互动。
- `AI热点追踪PoC/prd/AI热点发现与护卫军作业联动_PRD_v0.2.html`继续采用企业技术文档工作台样式，包含业务问题、闭环、分阶段能力、SMART、功能、架构、配置和验收，并已发布到`https://rondotang.github.io/dongfeng-push-demo/ai-hotspot-prd/`；当前发布提交为`ebdd500b5e3937aa1fa73ff1dce54958b7132ea0`，公网HTTP 200且线上与本地哈希一致。
- `AI热点追踪PoC/prd/AI热点业务规划与现阶段线索PoC解决方案_PRD_v0.2.md`明确阶段一只能基于公开信息发现线索、研判证据并生成两类作业草案，不能把搜索结果直接判为真实热点；未满足平台内容ID、原生指标、连续快照、UGC/作者标识和覆盖审计时，输出`unknown`及具体不可判定原因。
- 长期主数据只维护9个active品牌及别名；车型、人物、活动和机构由AI在事件中识别，证据不足时记录不确定原因并随事件人工审核。当前17条基础查询、配置规则、统一数据契约、运行编排、数据表和后台页面均已落地；原创发布结果登记、后效快照、增量观察和二次加热草案已进入本轮，正式作业下发、用户触达、互动执行及生产数据源接入仍不在本轮范围。
- 已把AI热点追踪从方案讨论推进到一次性实际PoC：新增`09_其他与待归档/0902业务沟通纪要/AI热点追踪PoC/`，用`热点采集规则_v0.1.yaml`固定品牌、近24小时、来源优先级、证据门槛、人工复核和排除规则。Codex公开搜索识别H01“岚图MPV与L3时代架构议题”为多来源扩散的外部议题，生成原创作业草案并保留高阶智驾口径人工门禁；H02“猛士X-First头号玩家计划”为官方新动作但未形成足够外部扩散，只归入品牌内容机会。此前误建的每天4次Codex定时任务已删除，当前无热点定时任务运行。
- 已读取`03_审核与AI中台/AI评论与直播话术生成/`的豆包链路及用户提供的Global版接口文档，在PoC目录新增`run_doubao_search.py`。接口固定为`POST https://open.feedcoopapi.com/search_api/global_search`、JSON请求体和Bearer鉴权；API Key通过`配置豆包搜索.command`隐藏写入macOS Keychain，配置检查通过，密钥未进入项目文件。当前只用于单次PoC，不启用定时任务。
- 2026-09-03已实际调用豆包Global Search共6次：4个品牌泛关键词返回11条去重结果，2个事件定向关键词返回6条去重结果。豆包有效补充东风官网近期动态、岚图齐鲁车展及“新能源从抢速度回归拼验证”等候选，但定向搜索漏掉Codex找到的当日岚图L3议题和猛士X-First官方微博，并出现旧闻、聚合页及实体混淆。当前PoC采用“豆包广召回＋Codex补证据/公开社交页面＋规则与人工最终判断”，详见`运行结果/2026-09-03_豆包与Codex对照结论.md`。
- 已按用户反馈放弃上一版模板风格，使用Presentations/Codex Grid重新生成`09_其他与待归档/0902业务沟通纪要/东风护卫军优质评论员运营与AI热点能力建设方案_咨询式重构版.pptx`。新版保留14页业务内容，但改为白底、黑白灰结构面与单一蓝色强调的咨询式汇报；删除彩色卡片堆叠、胶囊标签、底部总结条和通用Thank You页，每页只保留一个业务结论。全量渲染检查完成，PPTX结构校验通过，0个几何硬错误且无文本溢出；该版本作为当前优先评审版本。
- 已基于`09_其他与待归档/0902业务沟通纪要/`中的更新版Excel、会议纪要、PMO汇报、运营方案PDF及产品草稿，生成14页业务提案`东风护卫军优质评论员运营与AI热点能力建设方案_业务提案版.pptx`。汇报主线调整为“人工初筛优质评论员→公众号实操培养与优中选优→按擅长平台使用→结果助力尖兵/守卫提级”，平台能力标签沿用现有能力并优化更新机制，同时新增评论能力标签。AI热点部分明确从热点发现、研判到评论/原创/加热作业草案的闭环，并区分Codex、WorkBuddy、豆包搜索等PoC手段与正式企业级稳定采集能力；MCP仅作为AI与护卫军业务系统的内部调用接口。PPT已通过结构、版式、溢出和渲染检查，内容与图形均可编辑。
- 已将`09_其他与待归档/优质评论员选拔与运营_系统实施方案.md`收口为统一评论能力运营闭环。公众号实操维持“不改变发文流程、用户只传一次结果截图、系统绑定目标文章快照、AI观察、规则决策、精选/高赞人工确认”；限时赛一期只做统一开考单题，与评论大赛共用`ABSOLUTE_100`五维评分，个人计时随机题及练习行为均不进入一期、标签或排名。一期不建设反抄袭系统：只拦截同用户重复结算和相同截图、评论ID、唯一链接等明确重复凭证，不比较不同用户正文、不判断原创归属，也不做雷同分组、模糊相似、语义向量或模板聚类。能力评价使用独立`comment_value`，按质量分、活动系数、证据系数和人工确认效果计算，通过不可变正负事件账本、月度Top-N投影和最近90天汇总驱动尖兵/守卫；`reward_points`仅为可选奖励，不参与能力计算。留言接口仍仅为PoC增强项。该目录受现有`.gitignore`规则忽略，文件当前仅作为本地待评审资产。
- 双智能体接口指南已新增GitHub Pages根目录英文别名`ai-comment-live-agent-api-guide.html`，主访问地址为`https://rondotang.github.io/dongfeng-push-demo/ai-comment-live-agent-api-guide.html`，部署提交`80c326bd`。原中文路径保留兼容；新旧地址均返回HTTP 200，线上英文别名与本地文件SHA-256一致。Obsidian原有地址节点已增量更新，未新增本体关系或附件。
- 新增并发布`03_审核与AI中台/AI评论与直播话术生成/AI评论与直播话术生成_接口对接指南.html`，以独立开发指南形式定义两个智能体的串联契约：背景提取工作流接收8项业务入参并返回`output String`；话术生成智能体接收`content/scene_type/comment_style/num/word_count`并返回`comments Array<String>`。页面固定`output→content`原样映射、两段`scene_type`必填差异、确定性校验和部署待补项。文件已单独提交`73e1b6df`并发布至GitHub Pages，构建状态`built`、线上HTTP 200；Chrome Playwright、5段JSON示例、JavaScript语法及51项工作流测试通过。已在Obsidian新建`护卫军-AI评论与直播话术双智能体接口指南线上地址.md`，建立5条本体关系，无附件。
- 互动话术生成阶段已取消第二个HiAgent工作流，改为业务系统直接调用单个对话智能体Prompt。`prompts/interaction-generation-prompt.md`已从4314字符压缩至2076字符；`scene_type`改为可选，只有明确传入`live`才启用直播规则，为空、缺失、`null`或无法识别时默认按普通评论处理，兼容旧调用方。Prompt不再描述`content`的系统来源，仍保留事实边界、最少5字、`num=5`五类硬槽位、最大字数≤8时禁用逗号/分号/冒号及二段式等核心约束。生产快路径建议`temperature=0.6`、关闭推理模式、`max_tokens=512`，数量较多或长话术时再提高到1024；51项契约测试通过。
- 已为作业背景内容提取工作流建立HiAgent节点到本地文件的明确映射：`content-pack-workflow.md`主节点表新增“对应本地文件或配置”列，区分Prompt、Python、HiAgent原生Loop/Condition/HTTP/插件节点；将原先仅内嵌在导出YAML中的“公众号文章提取”和“总结和搜索分离”分别落为`code/wechat_article_extractor.py`与`code/summary_search_splitter.py`；同时标出当前YAML未接入的Bing、旧Fact Pack和来源策略历史脚本，防止后续误接。
- 已使用2026-08-14导出的HiAgent YAML和真实评论执行轨迹核对作业背景内容提取工作流：当前链路已端到端跑通并最终输出String总结；样本中最后总结LLM耗时19.621秒、2390 Tokens，可评估评论分支在官方总结后直接结束。分场景仍发生在全部素材提取之后，不会复制四类提取Prompt。`task_name/task_end_time`作为未引用冗余入参不计入性能优化；素材聚合Python执行短，也不作为主耗时。四类Prompt本体约为文本3000、外链3048、图片1910、视频2570字符，共同字段说明1072字符；文本复杂度与外链接近，后续应结合节点耗时和模型差异做A/B测试。
- 最终内容整合Prompt已改为普通评论与直播评论共用，入参统一为`official_input/web_input/task_start_time`，直接输出带固定段落标签和编号的纯文本总结，后续节点引用模型`raw_output`即可。新增“当前素材信息缺口”与“官方明确边界”的区分规则；豆包搜索结果按主体、来源、时态和高风险事实交叉印证筛选，冲突只剔除对应字段；禁止Markdown标题、宣传性结论和未经输入支持的计算，全文限制1600个中文字符。

- 用户将原始需求池第16行“反馈部分链接通过关联作业”和第40行“AI审核风险提醒”从`待组织原型评审`改为`上线跟踪`后，已重算全套运营视图。两条不再属于未来需求池：剩余需求45条、已有版本计划25条、尚未排版本10条、不确定10条、可组织评审16条、产品设计19条；`2026-08-31`集中需求从25条降至23条。AI能力与审核为20条（已有版本13、可评审9），内容与舆情运营为1条（已有版本1、可评审1）。
- 同步重建无宏点击钻取：总览仍有45个有效蓝色数字入口，`2.待决策清单`有44个精确分区及返回链接；已清理旧跳转目标，逐一验证显示数字等于目标分区条数。Sheet4的184条原始记录与用户更新版本逐单元格一致，公式/错误扫描为0，5张表完成渲染检查。
- 直播联网搜索使用豆包搜索Global API的HTTP节点，搜索Prompt按主题范围动态生成1～3条：精确车型或产品主题可以只生成1条；品牌历史、周年纪念、公益、赛事、文化和战略议题通常生成2条互补查询；存在第三个独立的技术、合作、政策、权益或事件缺口时最多3条。具体调试素材只进入测试，不写入生产Prompt，避免示例锚定。当前工作流的`summary_search_splitter.py`已确定性截断至3条。
- 新增`doubao_search_result_processor.py`，放在搜索词循环外，解析HiAgent HTTP节点的`body/status_code`信封；检查豆包`ResponseMetadata.Error/Result.ErrorCode`，合并`Snippet`文本，跨查询按URL去重并生成`W01`起的来源ID。节点只需1个入参`search_results`，固定返回`success/web_search_items_json/error_message`3个字段；已删除无业务消费的`queries/matched_queries/result_count/warnings_json`。`HostInfo.AuthorityLevel`仅作为供应商原始字段保存为`provider_authority_level`，Python不据此判断官方或事实可用性。用真实返回验证正常拆包。
- 新增`bing_search_crawl_params.py`，用于HiAgent搜索词循环内将BingSearch列表结果转换为`crawl_webpage`必需的`urlMap`Object和`uuids`String数组；优先解析`content[].json`，缺失时回退`content[].text`，按UUID和URL去重且保留搜索顺序。使用实际Bing返回中的10条结果验证两种路径输出一致；当前仅负责抓取参数拆包，相关性和来源分级仍在后续节点处理。
- 将AI评论与直播话术的内容资料包主链收敛为“业务素材聚合→官方资料总结→直播搜索补充”。`source_items_merger.py`只处理真实HiAgent信封、来源ID和数组聚合，不做语义判断；官方资料Prompt只输出`summary_for_display`和`search_queries`，直播按主题范围生成1～3条，评论场景固定为空数组。系统元数据、来源和查询编号仍由Python确定性补入。
- 为AI评论与直播话术联网搜索新增可直接上传HiAgent知识库的`knowledge-base/东风集团官方域名与可信车媒来源规则.md`：整理东风集团、自主/合资/商用车官方根域名及可信车媒路径级允许/降级范围，以31条`SOURCE_RULE|`机器行支持知识召回后的确定性解析；明确`search_policy`与`source_level`分离，降级来源仍可搜索抓取但只能作线索或交叉佐证。知识检索拆为“按本场实体召回官方域名”和“固定召回车媒允许/降级规则”两路，每个HiAgent知识检索节点只绑定一个`query`；前置Python负责由业务变量构造两个Query，后置Python直接接收两份`outputList`。新增`source_policy_query_builder.py`和`source_policy_parser.py`，并升级Bing筛选节点支持`trusted_url_rules/search_only_domains`。规则字段、唯一ID和枚举校验通过；完整29项单元测试通过。
- 扩展互动直播检测智能体的直播评论识别范围并重构联动流程：`category=live_stream`严格按“候选提取→目标确定→字段计算”执行。阶段A一次性扫描底部输入区、普通公屏和上墙/置顶展示区，只提取候选、区域和第一人称证据；阶段B优先查找非空`user_nickname`，未命中时与空入参一样使用可靠输入区昵称或“我/me”第一人称兜底，并只锁定一次目标；阶段C基于候选统一计算`user_comment_found/is_highlight/user_comment`，不再重新扫描或改选用户。非空入参启用兜底时返回`name_match_status=MISMATCHED`，但仍据实返回评论证据，无可靠第一人称才返回`NOT_FOUND`和评论未找到。该能力已独立冻结为`ali_bailian_prompt_增加上墙直播评论识别.md`和`hiagent_workflow_guide_增加上墙直播评论识别.md`；上墙版Prompt已移除真实测试图昵称，且保留两段增强Python。原`ali_bailian_prompt.md`已按用户提供的线上Prompt逐字恢复，原`hiagent_workflow_guide.md`也已换回线上单图拆包与循环外结算Python，保持双通道且不输出/校正`is_highlight`。契约测试同时验证基线与上墙版隔离，16项通过；两套本地文档均尚未重新发布到实际平台或GitHub Pages。
- 修正互动直播检测工作流的单图字段契约：`moments`与`invalid`不再返回直播专属的`is_duplicate/debug_info`，统一使用`status/reason`表达业务通过或失败；直播间账号不匹配不再伪装成重复图；`is_live_stream`表示“指定目标直播分享卡片”，必须同时满足直播体裁与目标直播间匹配，禁止使用朋友圈发布者身份为卡片借证，也禁止把“东风日产”等具体目标向上泛化成“东风”等集团通用词；`name_match_status`仅为身份匹配风险状态，不参与`is_live_stream/status/finalresult`结算；当`is_live_stream=false`时，`message/reason`返回直接原因；Python最终序列化前将字符串值双引号统一转为`「」`；`hiagent_workflow_guide.md`已补齐循环内HTTP与单图拆包Python；11项契约回归测试通过；HTML开发指南已发布到GitHub Pages提交`61c21e1`，但本轮Prompt收紧尚未同步到HTML或线上页面。
- 完成 2026-07-30 互动类人工复核生产回写：先成功试提交真实失败作业 `737634262609784832`，再从最终工作簿筛选 AF“可提交”的第 100～255 行共 156 条，按 35 条审核通过、106 条视频号直播失败、9 条公众号点赞+在看失败、6 条公众号评论失败拆成 10 批；接口返回成功 156、失败 0；35 条通过记录在同一审核请求内标记为 AI 不准确，失败记录未调用独立准确性接口；Excel 未修改，状态文件和日志不含 Token；新增 Antigravity Handoff，后续执行必须排除已提交 ID。
- 新增 2026-07-29 互动类人工复核批量提交工具：已反查并复现生产批量审核与 AI 准确性接口签名；原 107 条通过、360 条失败的全量提交计划已经失效并停用，不得再直接执行；2026-07-30 实际生产回写改为以 AF“可提交”及成功状态文件为准。
- 完成 2026-07-29 互动类 AI 审核失败全量复核：逐条审核 467 条提交、821 张图片；明确直播作业评论项设置为“均可”，仅以“东风汽车目标直播间”和“已发布朋友圈”为两项必选条件；本轮逐图复核 11 条争议记录，将 `737636992988413952`、`737637312741212160`、`737637736328118272`、`737633343772000256`、`737639294302388224`、`737640035922440192`、`737641450845712384`、`737633004754747392` 改为 AI 误判，保留分享内容错误的 `737641789045014528` 为真实失败；最终输出 115 条 AI 误判和 352 条真实失败；原 A～X 列及顺序不变，用户新增的 AF“提交状态”列 254 条非空数据原样保留。
- 为 2026-07-29 互动类 AI 审核失败全量复核新增图片断点下载脚本；Codex 已通过生产 `accountDetails` API 批量拉取 299 个直播用户，其中 298 个取得微信昵称，1 个绑定微信记录的昵称为空。
- 优化互动评论截图审核 Prompt：使用仅保留中英文和数字的机械昵称核心键消除图标/Emoji归属争论，并将“我/me”及其他匹配通道改为完整扫描、提取目标用户在截图中的全部评论。
- 优化互动直播检测智能体的评论提取 Prompt：新增平台入场/关注/分享/送礼等系统事件过滤，避免将“白龙马 来了”误判为用户主动评论。
- 在 `03_审核与AI中台/护卫军AI审核优化/互动评论打分智能体/` 建立评论额外奖励打分工作流，并新增对话智能体接入Prompt：业务系统直接写入`task_background/comments/ratio`三个智能体变量，对话智能体将其同名映射到工作流；工作流完成初评、五维排序与比例调节，再通过`output`原样返回。
- 互动评论配额Python节点已增加限定格式兼容层：支持数组对象误包引号、全部已定义字段名缺引号、尾随逗号和Python字面量格式；字段缺失、类型/枚举错误及截断等语义不明确问题仍通过`finalresult`返回结构化错误。
- 互动评论质量初评Prompt已增加主题类型适配：战略合作、品牌事件、公益和服务类任务使用相应主题细节，不再以车型或技术参数作为统一高分前提；补充非产品主题15分/10分边界示例。
- 互动评论外层调用智能体Prompt已补充字节级透传约束：禁止解析重组工作流`output`、删除转义符或修改引号字符，并明确区分中文左/右弯双引号U+201C/U+201D与英文直双引号U+0022；README同步声明生成式Prompt不能替代平台级工具结果直返。
- 外层智能体实测仍会把U+201C/U+201D改成未转义的U+0022；互动评论配额Python节点已增加确定性输出归一化，在序列化前把所有字符串值内的弯双引号、ASCII双引号和全角双引号转为`「」`，不改变JSON结构和评分逻辑。
- 全面扫描项目结构、69 份 Markdown 业务资料、主要 PRD/原型入口及 Obsidian 护卫军项目归档。
- 新增 `.agents/PROJECT_UNDERSTANDING.md`。
- 建立 `AGENTS.md` 和 `docs/project-memory/` 项目记忆入口。

## 下一步

- 使用本地后台连续运行7天，分别统计公开线索有效率、事件聚合准确率、人工研判时长、原创增长草案通过率和热点源内容加热草案通过率；不得把两类草案混为同一转化漏斗，也不得把搜索线索通过率解释为热点准确率。
- 运营确认哪些来源平台允许直接加热、各平台支持的点赞/正向评论等动作、审批责任人和频控要求；产品与研发据此冻结正式作业字段及后续护卫军作业接口。
- 启动生产数据源选型，重点验证未知链接前的平台内容发现、原生互动量、连续快照、作者/UGC标识、覆盖范围和SLA。达到热点判定数据准入条件后，再增加真实热点评级与自动候选规则。
- 优先由运营、公众号小编和产研共同评审评论能力运营方案，确认点赞效果档位、限时字数、评论力值系数与月度上限、尖兵/守卫名额、结果确认责任人、一次截图风险接受度及哪些活动另配奖励积分，再进入详细PRD与排期。
1. 联调前补齐双智能体的真实Endpoint、鉴权、工作流/智能体ID、平台响应Envelope和图片/视频文件上传协议；更新后同步线上指南与Obsidian节点。
2. 运营先用整理版确认45条剩余需求的模块与价值口径；业务主任对10条“不确定”逐条给出继续、补条件或退出结论，产品为10条“尚未排版本”补目标版本。另需核实23条集中填写`2026-08-31`的日期是否为真实同版承诺，再决定是否制作周级甘特。
3. 在HiAgent分别记录文本提取、官方总结、最终总结三个LLM的节点耗时与Tokens；若评论不需要二次排版，在现有场景判断后直接引用`summary_for_display`结束。使用同一批样本A/B测试精简文本Prompt与可选轻量模型，不因性能调整冗余入参、聚合Python或`MaxTokens`。
4. 将最新精简版`interaction-generation-prompt.md`配置到对话智能体，将生成模型改为`temperature=0.6`、关闭推理模式、`max_tokens=512`。分别用未传`scene_type`的旧评论调用和明确传`live`的直播调用复测，再用`num=5/word_count=5-8/comment_style=幽默、真诚`验收五个功能槽位、字数、短句结构与事实边界。
5. 将互动直播检测新版Python节点代码和Prompt复制到实际擎天/阿里视觉应用并发布；优先用“强小文上墙+冰蓝迪代截图”坏例复测，预期模型原始返回`name_match_status=MATCHED/user_comment_found=true/is_highlight=true`并提取强小文完整评论；再验证`live_room_name_match=false`时Python将最终`is_highlight`校正为`false`并追加单图与汇总原因。
6. 后续仅处理用户新标记的 AF“可提交”记录；必须先排除状态文件中的 156 个成功 ID和单条试提交 ID `737634262609784832`，重新分类并经用户确认后再提交。
7. 接到其他具体需求时，先确定其对应刚性节点和真实上线状态。
8. 涉及 AI 审核时，优先拉取坏例、库值、模型提取值和人工结论做证据链审计；互动评论打分需用历史样本验证 Top-K 排序与配额结果。
9. 涉及分发/标签/组织时，先确认现网数据模型与稳定 ID。

## 风险与阻塞

- 当前45条剩余需求中有10条尚未排版本、10条仍待业务确认；另有23条集中填写`2026-08-31`，可能包含占位日期。业务价值与版本承诺未逐条确认前，不应直接把该日期解释为同一版本的正式交付承诺。
- 工作区已有大量未提交的用户改动，后续修改需严格隔离。
- 作业背景内容提取工作流已完成一次评论场景端到端调试，但尚未完成纯作业名称、同时多素材、直播搜索成功/失败等完整回归；互动话术生成已完成String总结直接Prompt设计但尚未实机联调。Prompt不能100%保证JSON、数量和字数，业务系统至少需要解析、数量检查及失败重试；自动发布、计费或积分场景仍需确定性校验层。
- 素材聚合Python已完成图片/视频结果的来源ID生成，不再需要回填`file_name`或运营素材URL，也不重复定义语义枚举。仍待HiAgent实机联调图片/视频文件变量、模型直传能力、循环顺序稳定性，以及旧视频总结智能体是否需要二次结构化。
- 最终 Excel 第 100～255 行仍保留 `AF=可提交`，但对应 156 条已于 2026-07-30 成功提交；任何后续脚本如不读取状态文件去重，会造成重复审核风险。
- 生产技术栈、部署拓扑和 8—9 月节点当前实际完成度尚未在仓库中确认。

## 待核实

- 各刚性节点责任人、当前进度及验收结果。
- 生产 AI 审核指标和各场景坏例分布。
- 现网作业配置、标签分发、组织主数据和消息额度的真实实现。
